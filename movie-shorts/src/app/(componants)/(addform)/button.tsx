'use client'
import { Button } from '@mantine/core'
import React from 'react'

export default function button() {
  return (
    <div>
        <Button style={{ color: '#ffffff', background: 'linear-gradient(45deg, #FFD700, #FFA500)' }}>Upload</Button>
        <Button style={{ color: '#ffffff', background: 'linear-gradient(45deg, #FFD700, #FFA500)' }}>Upload</Button>
    </div>
  )
}
